import { SettingType } from './types'
export const Pool = new (class {
    constructor() {
    }

    get(): SettingType {
        return {};
    }

    set(value: SettingType): SettingType {
        return {};
    }

})(); 